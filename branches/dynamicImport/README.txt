This is the Ultimate reef Raspberry PI controller project.

The main python script is core.py.  It handles setting up threads for sensors and 
scheduling of  events.

The database folder contains the mysql workbench model for the Database and the 
sql create scrip tot create the DB.  These should be kept synched at all times (although
not the end of the world if they don’t).

The web UI code is contained in the XXXXXXX folder… 